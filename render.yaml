services:
  - type: web
    name: worklog
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: worklog-data
      mountPath: /data
      sizeGB: 1
